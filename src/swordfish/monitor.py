def monitor(toml_file, position, mt_key, frequency):
    # Upload initial TOML file
    while True:
        # Polling loop
        # Poll for update
        # If sha256sum is different update TOML_live file
        pass
